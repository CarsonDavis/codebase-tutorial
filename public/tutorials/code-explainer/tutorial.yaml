slug: code-explainer
name: Code Explainer
source:
  path: /Users/cdavis/github/code-explainer
  url: https://github.com/CarsonDavis/code-explainer
generated_at: "2026-05-07T19:30:00Z"
generator_version: "0.1.0"

summary: |
  A side-by-side viewer for code reviews where an agent's explanation lives next to the
  diff. Built on Next.js 15 App Router with Shiki syntax highlighting; pairs with an
  external coding agent that produces a review.json file.

components:
  - id: viewer
    title: Viewer
    summary: |
      The Next.js app that renders review.json as a three-column UI: file tree, diff,
      and explanation cards anchored to line ranges.
    type: subdivided
    sub_sections:
      - id: routing-and-shell
        title: Routing and shell
        summary: |
          Top-level page structure — the home page that lists reviews, the per-review
          SSG route, and the layout shell with the file-tree sidebar.
      - id: diff-rendering
        title: Diff rendering
        summary: |
          How the side-by-side diff is computed, how intra-line word highlighting
          works, and how unchanged context is truncated and lazily expanded.
      - id: explanation-cards
        title: Explanation cards
        summary: |
          How per-file overviews and per-change cards are composed and anchored to
          specific line ranges in the diff.
      - id: highlighting-and-settings
        title: Syntax highlighting and settings
        summary: |
          Shiki integration with lazy grammar loading, the customizable highlight
          palette, the settings menu, and the optional tour.

  - id: agent-contract
    title: Agent contract
    summary: |
      The contract for the coding agent that produces review.json — workflow, JSON
      schema, and tone rules. The "smart half" of the project.
    type: atomic

  - id: infrastructure
    title: Infrastructure
    summary: |
      Optional AWS CDK Python stack for hosting the static export. Example deploy code,
      not required for local use.
    type: atomic

  - id: ci-cd
    title: CI/CD
    summary: |
      GitHub Actions workflow that builds the static export and deploys it via the CDK
      stack. Optional alongside local-first usage.
    type: atomic

cross_refs:
  - from: agent-contract
    to: viewer/explanation-cards
    note: The cards are the front-end realization of the agent's ChangeExplanation schema.
  - from: agent-contract
    to: viewer/diff-rendering
    note: The diff is the surface the explanations are anchored to.
  - from: ci-cd
    to: infrastructure
    note: The workflow deploys via the CDK stack.
  - from: infrastructure
    to: ci-cd
    note: The CDK stack is invoked from the deploy workflow.
  - from: viewer/diff-rendering
    to: viewer/routing-and-shell
    note: Diff rendering is invoked from the per-file content stream owned by the shell.
  - from: viewer/diff-rendering
    to: viewer/explanation-cards
    note: Diff rows are the anchor target for explanation cards.
  - from: viewer/diff-rendering
    to: viewer/highlighting-and-settings
    note: Diff rendering consumes Shiki tokenization for code coloring.
  - from: viewer/explanation-cards
    to: viewer/diff-rendering
    note: Cards are positioned relative to diff row anchors.
  - from: viewer/explanation-cards
    to: viewer/routing-and-shell
    note: The shell mounts FileSection per file; cards are the per-file right column.
  - from: viewer/explanation-cards
    to: agent-contract
    note: Cards render ChangeExplanation and FileOverview as defined in the contract.
  - from: viewer/highlighting-and-settings
    to: viewer/diff-rendering
    note: Shiki tokens are consumed by the diff renderer.
  - from: viewer/highlighting-and-settings
    to: viewer/routing-and-shell
    note: The settings menu is mounted in the shell chrome.
  - from: viewer/routing-and-shell
    to: viewer/diff-rendering
    note: The shell's per-file content stream renders the diff for each file.
  - from: viewer/routing-and-shell
    to: viewer/explanation-cards
    note: The shell's per-file content stream renders explanation cards alongside the diff.
